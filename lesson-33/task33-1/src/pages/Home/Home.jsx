import { Container } from '@mui/material';
import Resume from '../../components/Resume/Resume';

const Home = () => {
	return (
		<Container maxWidth='lg'>
			<Resume />
		</Container>
	);
};

export default Home;
